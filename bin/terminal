#!/bin/bash

if [ "$#" -ne 1 ]; then
    echo "usage: $0 <site>"
    exit 1
fi

NUM_DAYS=30

gunzip -c logs-brandur/$1/*.gz | grep --line-buffered -v -E -f exclude_list.txt | goaccess - -p conf/goaccess.conf --keep-last $NUM_DAYS
